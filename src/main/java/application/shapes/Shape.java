package application.shapes;

public class Shape {
    Color color;
    Origin origin;

    public Shape(Color color, Origin origin) {
        this.color = color;
        this.origin = origin;
    }

}
